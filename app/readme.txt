## Use below command to execute program from the project's directory.
.\gradlew run  --args="src/main/resources/Example5.txt"
## Tried to add Checkstyle config, that was conflicting with my IDE, and never passed, hence commented.
## Added couple of tests, that could I have extended to achieve more code coverage.